-- computes the score average of all records in the table second_table

-- computes the score average of all records in table second_table as average
SELECT AVG(score) as 'average'
FROM second_table;
