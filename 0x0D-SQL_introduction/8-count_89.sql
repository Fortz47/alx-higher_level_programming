-- displays the number of records with id = 89 in
-- the table first_table of the database hbtn_0c_0 in your MySQL server

-- use database hbtn_0c_0
USE hbtn_0c_0;
-- displays the number or id=89 record
SELECT COUNT(id) FROM first_table;
